# se_assignment2
To use the two module in this repo 
1. run cmd under the folder you put this repo
2. run .venv\Scripts\activate in cmd
3. run pip install matplotlib in cmd
4. run python ExpenseTracker.py in cmd to insert expenses
5. run python GeneratePie.py in cmd to show pie chart of expenses by category
